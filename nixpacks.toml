[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r dashboard_html/requirements.txt"]

[start]
cmd = "cd dashboard_html && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
