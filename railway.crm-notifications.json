{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {"builder": "NIXPACKS"},
  "deploy": {
    "cronSchedule": "*/5 * * * *",
    "startCommand": "cd dashboard_html && python -u dispatch_crm_notifications.py",
    "restartPolicyType": "NEVER"
  }
}
